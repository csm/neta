{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                cancelCaptureSheet = id; 
                selectInterface = id; 
                selectMaxPackets = id; 
                selectSnapLength = id; 
                selectUseFilter = id; 
                showCaptureSheet = id; 
                startCapture = id; 
                stopCapture = id; 
            }; 
            CLASS = NADocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                allOrAny = NSPopUpButton; 
                cancelButton = NSButton; 
                captureButton = NSButton; 
                capturePanel = NSPanel; 
                capturingNumber = NSTextField; 
                capturingPanel = NSPanel; 
                capturingProgress = NSProgressIndicator; 
                capturingStop = NSButton; 
                filterEnabled = NSButton; 
                filtersBox = NSBox; 
                filtersView = NAFiltersView; 
                interfaceAddress = NSTextField; 
                interfaceAddress6 = NSTextField; 
                interfaceBox = NSBox; 
                interfaceDescription = NSTextField; 
                interfaces = NSPopUpButton; 
                loadingProgress = NSProgressIndicator; 
                loadingProgressPanel = NSPanel; 
                mainWindow = NSWindow; 
                numPackets = NSTextField; 
                numPacketsEnabled = NSButton; 
                packetDetail = NSOutlineView; 
                packetHex = NSTextView; 
                packetsTable = NSTableView; 
                promiscuous = NSButton; 
                snapLength = NSTextField; 
                snapLengthEnabled = NSButton; 
                statusLine = NSTextField; 
            }; 
            SUPERCLASS = NSDocument; 
        }, 
        {CLASS = NAFiltersView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = NAPCAPFilterView; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}